# REMOVED: Dockerfile removed in branch 'remove-docker'. This project uses Gradle for builds.
# To delete locally: git rm Dockerfile && git commit -m "chore: remove Docker files" && git push -u origin remove-docker

