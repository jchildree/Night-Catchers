# REMOVED: Dockerfile not used in this Android Kotlin project.
# This repository is an Android app; Docker/Python build artifacts were added accidentally and are not required.
# To remove this file entirely from the repo, run locally: git rm Dockerfile && git commit -m "chore: remove Dockerfile"

